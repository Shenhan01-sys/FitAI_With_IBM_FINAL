[build]
  command = "npm run build"
  publish = "dist/public"
  functions = "netlify/functions"

# Redirect all /api/* calls to the single Netlify function at /.netlify/functions/api
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
